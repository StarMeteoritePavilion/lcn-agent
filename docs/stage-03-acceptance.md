# 阶段 3 终端交互验收

验收及修复日期：2026-09-24。结论：**原两项失败已修复，交互脚本 20 项检查全部通过。**
阶段 3 仍保留终端体验覆盖边界，阶段 4 尚未开始。
用户授权处理后修改 `src/index.ts`，补充回归测试。全部请求使用本地模拟服务，没有调用真实模型。
环境：macOS 26.4.1、Node.js v25.9.0、npm 11.16.0、Python 3.14.4。

## 验证命令与结果

首次类型检查因本地缺少 `smol-toml` 失败；默认 `npm ci` 遇到 npm 缓存权限错误。
使用 `npm ci --cache /tmp/lcn-stage3-npm-cache` 按锁文件安装后，依赖恢复，锁文件没有修改。

| 实际命令 | 退出码 | 结果 |
| --- | --- | --- |
| `npm run check` | 0 | 类型检查通过 |
| `npm test` | 0 | 重新编译，2 项配置测试通过 |
| `python3 tests/terminal-cancel.py` | 0 | 空输入、取消时冲刷半行、取消后继续、空闲 Ctrl+C、退出通过 |
| `/tmp/lcn-stage3-acceptance-venv/bin/python tests/terminal-interaction.py` | 0 | 修复后 20 项检查全部通过；修复前 13 项通过、2 项失败，连续两次复现 |
| `git diff --check` | 0 | 已跟踪文档的差异检查通过 |

新增脚本使用 Python PTY、本地 HTTP 服务及 `pyte==0.8.2` 终端仿真器。
`pyte` 只装在临时虚拟环境，未增加应用依赖。重跑方式：

```bash
npm run build
python3 -m venv /tmp/lcn-stage3-acceptance-venv
/tmp/lcn-stage3-acceptance-venv/bin/python -m pip install pyte==0.8.2
/tmp/lcn-stage3-acceptance-venv/bin/python tests/terminal-interaction.py
```

适用 macOS/Linux，要求 Node.js、Python、`/bin/bash`。新脚本在临时目录生成固定测试配置，
不会读取项目 `.env`。旧取消脚本继续使用项目配置及注入的测试环境变量。

## 已取得的证据

- 中文输入、2100 字符中文长行：服务端收到的消息与发送内容逐字相等。
- 多行粘贴：两行分别成为两次独立请求，顺序和内容准确；不等于支持单条多行消息。
- Markdown：仿真屏幕中中文标题、加粗与颜色属性存在，中文占用双列。
- 缩放：覆盖短中文编辑行及 75 字符中文长编辑行；终端宽度在 40、80、100 列间切换后，编辑行、光标和退格提交正确。
- 生成结束：短输入和跨行长输入均保留；模型输出独立显示；退格、中间光标保留与插入后的服务端消息均符合预期。
- 工具：展示 echo 开始、中文结果和第二轮完成；服务端消息中的 assistant 工具调用与 tool 结果按 ID 配对。
- 退出：`/exit` 和空闲 Ctrl+C 后，PTY 的 `termios` 属性与启动前一致。
- Shell 恢复：退出后输入带退格的 Shell 命令，实际生成文件且内容正确，证据不是命令回显。
- 非交互：命令行参数正确拼接为 `命令行 中文`，无交互引导，完成后退出码为 0。

## 修复前的两项失败与复现步骤

1. 提交 `生成期间编辑`，模拟服务暂停返回。
2. 输入 `下一条错`，不按回车。
3. 放行服务端，输出 `模型输出第一行`、`模型输出第二行` 并结束响应。
4. 在恢复的输入提示处按一次退格，输入 `对`，再按回车。

| 检查 | 预期 | 实际 |
| --- | --- | --- |
| 生成输出与编辑行分离 | 模型第一行独立显示 | 屏幕出现 `下一条错模型输出第一行` |
| 继续编辑后提交 | 服务端收到 `下一条对` | 服务端收到 `对下一条错` |

根因：渲染函数直接写 stdout，未协调 readline 正在编辑的行；
`showPrompt()` 调用无参数的 `input.prompt()`，会把编辑光标重置到开头。

修复：展示层合并同一运行事件的输出，通过统一回调写入；交互入口利用 Node 原生
`getCursorPos()`、`moveCursor()`、`cursorTo()`、`clearScreenDown()` 在编辑行上方输出，
再用 `prompt(true)` 保留原光标并重绘。模型、工具、用量、错误和用户消息共用此路径；
非交互错误仍写 stderr。未引入应用依赖。

取消测试原先用提示符判断请求结束；现在生成中也会重绘提示符，因此改为等待实际取消或完成文本，
保留半行冲刷、继续请求及退出断言。

## 覆盖边界与下一步

本次已验证短编辑行和 75 字符中文跨行输入的缩放重绘、生成结束后的光标保留及中间插入。
尚未验证超过一屏的编辑内容、完整滚屏历史、各终端软件的字体与颜色一致性，以及全部 Markdown 结构。
生成中取消沿用现有测试；终端属性比较覆盖 `/exit` 和空闲 Ctrl+C，未单独比较生成中取消后的属性。

原两项缺陷已修复，不再作为待办。阶段 3 总勾选暂不改变，下一节点明确上述剩余体验边界并补充专项验收；
本次不推进会话持久化。
